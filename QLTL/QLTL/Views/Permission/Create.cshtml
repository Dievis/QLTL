@model QLTL.ViewModels.PermissionVM.PermissionViewModel

@{
    ViewBag.Title = "Tạo quyền";
}

@section styles {
    <link href="~/Content/createEdit.css" rel="stylesheet" />
}

<div class="container mt-4">
    <div class="card shadow">
        <!-- Header -->
        <div class="card-header card-header-gradient d-flex justify-content-between align-items-center">
            <h2 class="mb-0 text-white">
                <i class="bi bi-plus-circle me-2"></i> Tạo quyền
            </h2>
            <a href="@Url.Action("Index", "Permission")" class="btn btn-light">
                <i class="bi bi-arrow-left"></i> Quay lại
            </a>
        </div>

        <!-- Body -->
        <div class="card-body">
            @using (Html.BeginForm("Create", "Permission", FormMethod.Post, new { @class = "needs-validation", novalidate = "novalidate" }))
            {
                @Html.AntiForgeryToken()

                <!-- Tên quyền -->
                <div class="mb-3">
                    @Html.LabelFor(m => m.PermissionName, new { @class = "form-label" })
                    @Html.TextBoxFor(m => m.PermissionName, new { @class = "form-control", placeholder = "Nhập tên quyền" })
                    @Html.ValidationMessageFor(m => m.PermissionName, "", new { @class = "text-danger" })
                </div>

                <!-- Mô tả -->
                <div class="mb-3">
                    @Html.LabelFor(m => m.Description, new { @class = "form-label" })
                    @Html.TextAreaFor(m => m.Description, new { @class = "form-control", rows = 3, placeholder = "Nhập mô tả (không bắt buộc)" })
                </div>

                <!-- Quyền mặc định -->
                <div class="mb-3 form-check">
                    @Html.LabelFor(m => m.IsDefault, new { @class = "form-check-label" })
                    @Html.CheckBoxFor(m => m.IsDefault, new { @class = "form-check-input" })
                </div>

                <!-- Nút hành động -->
                <div class="d-flex justify-content-start mt-4">
                    <button type="submit" class="btn btn-success">
                        <i class="bi bi-check-circle"></i> Lưu
                    </button>
                    <a href="@Url.Action("Index", "Permission")" class="btn btn-secondary">
                        <i class="bi bi-arrow-left"></i> Hủy
                    </a>
                </div>
            }
        </div>
    </div>
</div>
